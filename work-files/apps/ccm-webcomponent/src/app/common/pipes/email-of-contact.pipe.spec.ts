import { EmailOfContactPipe } from './email-of-contact.pipe';

describe('EmailOfContactPipe', () => {
  it('create an instance', () => {
    const pipe = new EmailOfContactPipe();
    expect(pipe).toBeTruthy();
  });
});
