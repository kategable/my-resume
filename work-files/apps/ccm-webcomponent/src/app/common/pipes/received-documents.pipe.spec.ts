import { ReceivedDocumentsPipe } from './received-documents.pipe';

describe('ReceivedDocumentsPipe', () => {
  it('create an instance', () => {
    const pipe = new ReceivedDocumentsPipe();
    expect(pipe).toBeTruthy();
  });
});
